# =============================================================================
# requirements.txt — Component Detection Backend
# Generated from actual imports in:
#   app/main.py, app/ml_pipeline.py, app/models.py,
#   app/schemas.py, app/database.py
#
# Setup: see the root README.md for full first-time setup instructions.
# NOTE: torch/torchvision/torchaudio below are CUDA 11.8 builds (the
# "+cu118" suffix) — they are NOT on the default PyPI index. The line below
# adds PyTorch's own index as a fallback so a plain `pip install -r
# requirements.txt` resolves them correctly in one shot.
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cu118

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.136.3
starlette==1.2.1
uvicorn==0.49.0
python-multipart==0.0.32
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
idna==3.18

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# ── Database (SQLAlchemy + SQLite) ────────────────────────────────────────────
SQLAlchemy==2.0.50
greenlet==3.5.1

# ── ML / Computer Vision ─────────────────────────────────────────────────────
# PyTorch — CUDA 11.8 build (keep GPU support)
torch==2.7.1+cu118
torchvision==0.22.1+cu118
torchaudio==2.7.1+cu118

# YOLOE (THU-MIG fork — pinned commit used at development time)
ultralytics @ git+https://github.com/THU-MIG/yoloe.git@40cd606cabdbe2b566d6f14a6b162c89206e9a1b
ultralytics-thop==2.0.20

# OpenCV
opencv-python==4.13.0.92

# Pillow (PIL)
pillow==12.2.0

# NumPy
numpy==2.4.6

# Supervision (bounding box utilities)
supervision==0.28.0

# ── Supervision / Ultralytics transitive deps ─────────────────────────────────
matplotlib==3.10.9
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0

scipy==1.17.1
scikit-learn==1.9.0
joblib==1.5.3
threadpoolctl==3.6.0

pandas==3.0.3
pyarrow==24.0.0
tzdata==2026.2

# ── Hugging Face (pulled in by ultralytics) ───────────────────────────────────
huggingface_hub==1.18.0
safetensors==0.7.0
tokenizers==0.22.2
transformers==5.10.2
hf-xet==1.5.0
filelock==3.29.1
fsspec==2026.4.0
packaging==26.2
requests==2.34.2
urllib3==2.7.0
certifi==2026.5.20
charset-normalizer==3.4.7

# ── TIMM (vision model zoo — ultralytics dep) ─────────────────────────────────
timm==1.0.27

# ── EasyOCR (component marking/rating identification) ─────────────────────────
easyocr==1.7.2
python-bidi==0.6.11
Shapely==2.1.2
pyclipper==1.4.0
ninja==1.13.0
scikit-image==0.26.0
imageio==2.37.3
tifffile==2026.3.3
lazy-loader==0.5

# ── Async / Network ───────────────────────────────────────────────────────────
aiohttp==3.14.1
aiosignal==1.4.0
aiohappyeyeballs==2.6.2
attrs==26.1.0
frozenlist==1.8.0
multidict==6.7.1
propcache==0.5.2
yarl==1.24.2

# ── Misc Utilities ────────────────────────────────────────────────────────────
PyYAML==6.0.3
tqdm==4.68.1
regex==2026.5.9
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.4.1
colorama==0.4.6
setuptools==81.0.0
xxhash==3.7.0
